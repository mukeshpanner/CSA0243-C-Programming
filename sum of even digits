#include<stdio.h>
#include<conio.h>
int main()
{
 int a,n;
 int sum =0;
 printf("enter the given number:");
 scanf("%d",&n);
 clrscr();
 while (n!=0)
 {
  a = n%10;
  sum =sum + a;
  n=n/100;
 }
  printf("sum of the even digits is %d\n",sum);
  return 0;
}
